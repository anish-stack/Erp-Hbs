# ERP gateway base URL (all REST goes through here)
NEXT_PUBLIC_API_BASE=http://127.0.0.1:4000
# Notification service (Socket.IO connects directly, not via gateway)
NEXT_PUBLIC_NOTIFICATIONS_URL=http://127.0.0.1:4015
